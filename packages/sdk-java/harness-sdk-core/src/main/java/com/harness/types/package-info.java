/**
 * Core type definitions for Harness SDK Java.
 *
 * These types form the foundation of the agent loop, tool system, and memory management.
 *
 * Key types:
 * - {@link Message}: A single message in the conversation
 * - {@link Session}: A conversation session with messages and state
 * - {@link ToolCall}: A tool call request from the LLM
 * - {@link ToolResult}: Result from a tool execution
 * - {@link LLMResponse}: Response from LLM
 * - {@link LoopResult}: Result from agent loop execution
 * - {@link TokenUsage}: Token usage statistics
 * - {@link LoopState}: Agent loop state machine states
 * - {@link StopReason}: LLM response stop reason
 */
package com.harness.types;
